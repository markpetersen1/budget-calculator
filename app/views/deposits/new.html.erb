<h1>New Deposit</h1>

<%= form_with model: @deposit, local: true do |form| %>
 
  <% if @deposit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@deposit.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @deposit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>
 
  <p>
    <%= form.label :amount_per_month %><br>
    <%= form.text_area :amount_per_month %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>

<% end %>