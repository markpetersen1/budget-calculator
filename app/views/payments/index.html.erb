<h1>Your payments:</h1>
<h2>These are the bills you are currently paying on a monthly basis</h2>

<table>
  <tr>
    <th>Title</th>
    <th>Amount Per Month</th>
    <th></th>
  </tr>
 
  <% @payments.each do |payment| %>
    <tr>
      <td><%= payment.title %></td>
      <td><%= payment.amount_per_month %></td>
      <td><%= link_to 'Show', payment_path(payment) %></td>
      <td><%= link_to 'Edit', edit_payment_path(payment) %>
      <td><%= link_to 'Destroy', payment_path(payment), 
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<br>
<%= link_to 'New recurring payment', new_payment_path %>
<br><br>
<%= link_to 'Back to Home', controller: 'welcome' %>